# Guardrails NADF — sample2

- Scope: este repositorio únicamente (`src/`, `tests/`, `template.yml`).
- Entorno permitido: `dev`. Deploy a producción: **prohibido**.
- Prefijos de recursos AWS: `sample2-*`.
- Runtime Lambda: Python 3.12 + AWS SAM HTTP API.
- No añadir secretos, AWS keys ni archivos `.env` con valores reales.
- Commit directo a `main` permitido en modo automático NADF.
